# Diario dello sviluppatore, data 13 dicembre 2025

* [index](../index.md) |
  [To Do List](../TDL.md) da svuotare e migrare in |
  [project](https://github.com/users/mrai64/projects/1) issue list

* [Route](/routes/web.php)

* [◀️ Diario di ieri](./2025-12-12_IT.md)

* run <http://yapcp.test/>
* [log](/storage/logs/laravel-2025-12-13.log) | ngnix log  `open ~/Library/Application\ Support/Herd/Log`

## Pagina Assegnazione premi per concorso

La pagina dve esporre i premi da assegnare per il solo
concorso (e queste si distinguono dai premi di sezione
perché manca il codice sezione), e un elenco di premi assegnati
per le sezioni, ciascuna il suo. Quindi mi serve inserire
un nuovo componente divide-et-impera falla-semplice

```sh
$ php artisan livewire:make Organization/Award/ContestAssignedDia
 COMPONENT CREATED  🤙

CLASS: app/Livewire/Organization/Award/ContestAssignedDia.php
VIEW:  resources/views/livewire/organization/award/contest-assigned-dia.blade.php
```

[CLASS:](/app/Livewire/Organization/Award/ContestAssignedDia.php)
[VIEW:](/resources/views/livewire/organization/award/contest-assigned-dia.blade.php)

```sql
select
`pcp_contest_awards`.`award_code`,
`pcp_contest_awards`.`award_name`,
`pcp_countries`.`flag_code`,
`pcp_countries`.`id`,
`pcp_user_contacts`.last_name,
`pcp_user_contacts`.first_name,
`pcp_works`.title_en,
`pcp_works`.reference_year,
`pcp_works`.work_file
from pcp_works
left join pcp_contest_awards on (pcp_works.id = pcp_contest_awards.winner_work_id) 
left join pcp_user_contacts on (`pcp_contest_awards`.`winner_user_id` = `pcp_user_contacts`.user_id)
left join pcp_countries on (`pcp_user_contacts`.country_id = `pcp_countries`.`id`)
where pcp_contest_awards.section_id = 'cddbc976-e318-427e-ac88-5cb5f7401012'
order by pcp_contest_awards.award_code;
```

diventa secondo gemini 

```php
use Illuminate\Support\Facades\DB;

$results = DB::table('pcp_works')
    ->leftJoin('pcp_contest_awards', 'pcp_works.id', '=', 'pcp_contest_awards.winner_work_id')
    ->leftJoin('pcp_user_contacts', 'pcp_contest_awards.winner_user_id', '=', 'pcp_user_contacts.user_id')
    ->leftJoin('pcp_countries', 'pcp_user_contacts.country_id', '=', 'pcp_countries.id')
    ->select(
        'pcp_contest_awards.award_code',
        'pcp_contest_awards.award_name',
        'pcp_countries.flag_code',
        'pcp_countries.id', // Note: Using pcp_countries.id as the column name
        'pcp_user_contacts.last_name',
        'pcp_user_contacts.first_name',
        'pcp_works.title_en',
        'pcp_works.reference_year',
        'pcp_works.work_file'
    )
    ->where('pcp_contest_awards.section_id', 'cddbc976-e318-427e-ac88-5cb5f7401012')
    ->orderBy('pcp_contest_awards.award_code')
    ->get();
```

Risultato notevole, adesso manca la parte "questo premio a chi lo dò"?
